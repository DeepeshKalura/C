/*
    Write a program in C to find the largest and smallest word in a string. 

#include <stdio.h>
#include <string.h>

int main()
{
    char a[1000], max[100], min[10], b;
    gets(a)
    for(int i = 0; i<strlen(a); i++)
    {
        if(a[i]!=' ')
        {
            max 
        }
        
    }
    return 0;
}
*/

// complete can be done.